require(settings)
#' @export
daRtOpts <- settings::options_manager(minBuildVersion = 1091, maxBuildVersion = Inf)
#hereiam - need to continue and check this dartopts - add it as validity check for simhanldle
